# Regen Accelerator — environment template.
# Phase 1 (static learning surface) needs nothing here. Copy to `.env` and fill
# in as later phases add dynamic behavior.

# PUBLIC_SITE_URL=https://regen-accelerator.example
