{
  "name": "regen-acc-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Regen Accelerator — monorepo container. Phase 1: the Request for Regens learning surface (apps/web).",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @regen/web dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=22"
  }
}
